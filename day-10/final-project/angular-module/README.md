# Angular Module — Hygiene Risk Predictor

Functional Angular standalone page that posts facility features to the FastAPI `/predict` endpoint from `backend/main.py`.

## Run
```bash
# Terminal 1 — ML API
cd ../backend && python main.py

# Terminal 2 — Angular module
npm install
npm start
```
Open http://localhost:4300
